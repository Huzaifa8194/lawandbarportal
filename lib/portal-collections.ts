/**
 * Portal-only Firestore collections. Keep these separate from the main marketing site
 * so student content never syncs to public `books` (or other) collections.
 */
export const PORTAL_BOOKS_COLLECTION = "portal_books";
